---
image: golang:latest

variables:
  # Please edit to your GitLab project
  REPO_NAME: github/kofemann/autoca

stages:
  - test
  - build

format:
  image: golang:latest
  stage: test
  before_script:
    - cd $GOPATH/src
    - mkdir -p $REPO_NAME
    - ln -sv $CI_PROJECT_DIR $REPO_NAME
    - cd $GOPATH/src/$REPO_NAME
    - go mod init
    - go get gopkg.in/yaml.v2
  script:
    - go fmt $(go list ./...)
    - go vet $(go list ./...)
    - go test -race $(go list ./...)

compile:
  image: golang:latest
  stage: build
  before_script:
    - cd $GOPATH/src
    - mkdir -p $REPO_NAME
    - ln -sv $CI_PROJECT_DIR $REPO_NAME
    - cd $GOPATH/src/$REPO_NAME
    - go mod init
    - go get gopkg.in/yaml.v2
  script:
    - go build -race -ldflags "-extldflags '-static' -X main.revision=`git describe --always --long --dirty`" -o $CI_PROJECT_DIR/autoca
  artifacts:
    paths:
      - autoca
